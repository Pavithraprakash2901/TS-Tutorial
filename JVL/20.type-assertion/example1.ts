const element = document.getElementById('myElement') as HTMLInputElement

element.value = 'hello typescript';
element.name = 'enna gopi ethulam...!!!'

// this used to change the dom elements which is used on the certain part of area

